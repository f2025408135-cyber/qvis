fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0
sqlalchemy[asyncio]>=2.0.0
structlog>=24.1.0
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.31.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
playwright>=1.40.0
prometheus-fastapi-instrumentator>=6.1.0
prometheus-client>=0.19.0

# Optional: PostgreSQL backend (production)
# pip install asyncpg                    # Async PostgreSQL driver for SQLAlchemy

# Optional: Multi-platform collectors
# pip install boto3                    # AWS Braket
# pip install amazon-braket-sdk        # AWS Braket device details
# pip install azure-quantum            # Azure Quantum
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
redis>=5.0.0
